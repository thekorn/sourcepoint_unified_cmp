name: sourcepoint_unified_cmp_ios
description: iOS implementation of the sourcepoint_unified_cmp plugin
version: 0.1.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

flutter:
  plugin:
    implements: sourcepoint_unified_cmp
    platforms:
      ios:
        pluginClass: SourcepointUnifiedCmpPlugin
        dartPluginClass: SourcepointUnifiedCmpIOS

dependencies:
  flutter:
    sdk: flutter
  sourcepoint_unified_cmp_platform_interface: ^0.1.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4
  pigeon: ^16.0.0
  plugin_platform_interface: ^2.0.0
  very_good_analysis: ^5.1.0
